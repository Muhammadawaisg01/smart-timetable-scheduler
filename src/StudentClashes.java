import Model.Section;
import Model.Student;
import java.util.ArrayList;

public class StudentClashes {
    /*
    @param section: section
    
    */
    public static void resolveStudentClash(Section section, int trigger) {
        ArrayList<Student> students = new ArrayList<>();
        // take each course of section
            // get students having clash with course and section
            // number of students > trigger
                // create copy of section.schedule array
                // 
                
                    
    }
}
